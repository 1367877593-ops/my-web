const loader=document.getElementById('loader');window.addEventListener('load',()=>setTimeout(()=>loader?.classList.add('hide'),650));const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const heroVideo=document.querySelector('.hero-photo video');if(heroVideo){if(reduced){heroVideo.removeAttribute('autoplay');heroVideo.pause()}else{const playHero=()=>heroVideo.play().catch(()=>{});playHero();heroVideo.addEventListener('canplay',playHero);document.addEventListener('visibilitychange',()=>{if(!document.hidden)playHero()})}}if(!reduced){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1,rootMargin:'0px 0px -40px'});document.querySelectorAll('.section-head,.about-grid,.cap-grid article,.project-list article,.article-list a,.collab-grid article,.contact form').forEach(el=>{el.classList.add('reveal');io.observe(el)})}const form=document.getElementById('contactForm');form?.addEventListener('submit',e=>{e.preventDefault();const b=form.querySelector('button'),t=b.textContent;b.textContent='消息已发送 ✓';setTimeout(()=>b.textContent=t,2200);form.reset()});
